# rofi command used in most of the scripts
# rofi -dmenu -i
# rofi -dmenu -i -kb-accept-entry 'Alt+S'
# rofi -dmenu -i -kb-screenshot Control+Shift+space"
#ROFI_CMD="rofi -dmenu -i"

# path to rofi-desktop data directory
#ROFI_DATA_DIR=/path/to/data

# path to rofi-desktop config directory
#ROFI_CONFIG_DIR=/path/to/config

# path to rofi-desktop cache directory
#ROFI_CACHE_DIR=$HOME/.cache

# default terminal emulator
#TERMINAL="xterm"

# show icons in rofi-filebrowser, window switcher and custom menus, set empty to disable icons
#SHOW_ICONS="-show-icons"

# show windows thumbnails in window switcher, set empty to disable thumbnails
#SHOW_WINDOW_THUMBS="-window-thumbnail"

# show windows thumbnails in a grid, set empty show in list
#SHOW_THUMBNAILS_GRID="yes"

# task manager command
#TASK_MANAGER="xterm -e htop"

# system info command
#SYSTEM_INFO="inxi -c0 -v2"  # neofetch --stdout --color_blocks off

# code projects folder and preferred editor
#PROJECTS_DIRECTORY="~/Programs"
#PROJECTS_EDITOR="qtcreator"

# output folders of video and audio recordings
#VIDEO_FOLDER="$HOME/Videos/record"
#AUDIO_FOLDER="$HOME/Music/record"
#VIDEO_CODEC="libvpx" # h264

# flathub cache file and expiration time to refresh it
#FLATHUB_EXPIRATION_TIME=3600

# latest news rss cache file and expiration time
#RSS_EXPIRATION_TIME=600

# notes author, path to file and default editor
#NOTES_AUTHOR="$(whoami)"
#NOTES_EDITOR="xdg-open"

# radio stations file, cache file and player command
#RADIO_PLAYER="mpv --no-resume-playback --force-window=immediate"

# tv stations file, cache file and player command
#TV_PLAYER="mpv --no-resume-playback --force-window=immediate"

# screenshots filename template (used by scrot)
#SCREENSHOT_NAME="Screenshot_%Y-%m-%d-%S-%H%M%S.png"

# search for hidden files
#SHOW_HIDDEN_FILES=false

# search history file path and max size
#MAX_HISTORY_ENTRIES=100

# show context characters in file contents search, set empty to show only filename
#SHOW_CONTEXT="yes"

# image thumbnails grid size options
#GRID_ROWS=3
#GRID_COLS=5
#ICON_SIZE=6

# screen locker in session management
#USE_LOCKER=false
#LOCKER="i3lock"

# steam games path and caches
#STEAM_ROOT="$HOME/.local/share/Steam"

# timer audio effects and notification timeout
#TIMER_START_AUDIO="path/to/start_audio"
#TIMER_STOP_AUDIO="path/to/stop-audio"
#TIMER_NOTIFICATION_TIMEOUT=5000

# path to wallpapers directory
#WALLPAPERS_DIR=$HOME/Pictures

# systemd unit default action
#SYSTEMD_DEFAULT_ACTION="list_actions" # enable/disable/stop/restart/tail

# livetv events list file and expiration time
#LIVETV_EXPIRATION_TIME=3600

# podcast player, folder, cache location and expiration time
#PODCAST_PLAYER=-mpv --no-resume-playback --force-window=immediate
#PODCAST_EXPIRATION_TIME=3600

# default torrent client and cache for searches
#TORRENT_CLIENT=qbittorrent

# livetv sports events cache location and expiration time
#LIVETV_EXPIRATION_TIME=3600

# keypassxc database file
#KEEPASSXC_DATABASE=path/to/database

# xkcd comics cache locations and expiration time
#XKCD_EXPIRATION_TIME=86400 # refresh xkcd file every day
#XKCD_ICON_SIZE=${XKCD_ICON_SIZE:-35}

# github search cache and default clone folder
#CLONE_FOLDER="$HOME/Downloads/"

# custom menus location
#CUSTOM_FOLDER=path/to/menus
